@using Kendo.Mvc.UI
@using Models
@using Models.Entities
@model int
@(
        Html.Kendo().ComboBoxFor(m => m)
            .Filter("contains")
            .Placeholder("Chọn Lớp học")
            .Suggest(true)
            .DataValueField("ClassId")
            .DataTextField("ClassName")
            .BindTo((IQueryable<LearningClass>)ViewBag.LearningClasses)
)